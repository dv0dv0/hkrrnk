#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    int t;
    cin >> t;
    for(int a0 = 0; a0 < t; a0++){
        int n, L;
        string nString;
        cin >> n;

//      convert num to string, then find length
        ostringsstream convert;
        convert << n;
        nString = convert.str();
        L = nString.length();

//      loop through string, check divisibility
        for (int x = 0; x < L; x++) {
            

        }

    }
    return 0;
}
